import { usePathname } from "next/navigation";
import title, {
    FormTitleError as titleError,
  } from "./UI/auth/foreGround/text";


export const FormEnglober = () => {
    const path = usePathname()

    return  (
        <div></div>
    )
}